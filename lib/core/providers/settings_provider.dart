// alertThresholdProvider and appConfigProvider live in config_repository.dart.
// Re-export here so existing imports of settings_provider still compile.
export 'package:hisaab_kitaab/core/repositories/config_repository.dart'
    show alertThresholdProvider, appConfigProvider;
